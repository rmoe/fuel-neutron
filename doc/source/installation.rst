============
Installation
============

At the command line::

    $ pip install fuel_neutron

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv fuel_neutron
    $ pip install fuel_neutron